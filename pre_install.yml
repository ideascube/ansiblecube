---
- name: Check if the software has been installed, if no, we create an entry with version = 0
  ini_file: dest=/etc/ansible/facts.d/installed_software.fact
    section=software
    option={{ task }}
    value=0
  when: ansible_local.installed_software.software.{{task}} is undefined

- name: reload ansible_local
  setup: filter=ansible_local