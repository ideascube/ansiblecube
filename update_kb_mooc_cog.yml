---
# Ansible playbook for KoomBook EDU RDC

#It will be always localhost
- hosts: localhost

  roles:
    # Has to be always enable
    - logs
    - tinc-static
    # - role: upgradeIdc
    #   version: "0.9.0"

