#!/bin/bash

IF=$1
STATUS=$2

if [[ "$IF" == eth* || "$IF" == "{{client_interface}}" ]]
then
    case "$STATUS" in
        up)
            curl -sfL https://raw.githubusercontent.com/ideascube/ansiblecube/oneUpdateFileBuster/roles/system/files/system-upgrade.sh | bash
        ;;
        *)
            exit 0;
        ;;
    esac
fi
