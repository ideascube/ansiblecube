case "$reason" in
    BOUND|RENEW|REBIND|REBOOT|TIMEOUT)

    sleep 120 && rm -rf /var/lib/ansible/ && /usr/local/bin/ansible-pull -d /var/lib/ansible/local -i hosts -U https://github.com/ideascube/ansiblecube.git initPullUpdate.yml >> /var/log/ansible-pull.log 2>&1 &

    ;;
esac